# Contains-Duplicates
checking whether the list of numbers have duplicates or not using hashing
#Algorithm
-create an empty set named "seen"
-if number is seen-return True as there is a duplicate
-else add to seen
-no duplicates found -return False
#Complexity:
time=O(n)
space=O(1)
